spring:
  data:
    mongodb:
      database: mtg-collection
      uuid-representation: standard

springdoc:
  swagger-ui:
    defaultModelsExpandDepth: -1
    path: /doc

scryfall:
  base-url: "https://api.scryfall.com"

server:
  port: 9090
